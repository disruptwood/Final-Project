# Presentation folder

- `deck.md` — single source of truth for slide-by-slide narrative (B2C).
- `assets/` — images referenced by `deck.md`.

Generate assets (optional):
- `MPLBACKEND=Agg MPLCONFIGDIR=/tmp/mplconfig python3 scripts/make_graphs.py`
